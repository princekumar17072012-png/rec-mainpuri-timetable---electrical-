import {specialDays} from '../data/specialDays';
export function SpecialDays(){const grouped=specialDays.reduce<Record<string,typeof specialDays>>((acc,x)=>(acc[x.date.slice(0,7)]??=[]).push(x)&&acc,{});return <div className="page"><div className="page-title"><span className="eyebrow">CALENDAR</span><h1>Special days</h1><p>Selected national days, festivals and occasions for the 2026–27 session.</p></div>{Object.entries(grouped).map(([month,items])=><section className="month" key={month}><h3>{new Date(month+'-01T00:00:00').toLocaleDateString('en-IN',{month:'long',year:'numeric'})}</h3>{items.map(x=><div className="calendar-row" key={x.date}><span>{x.icon}</span><div><b>{x.title}</b><small>{new Date(x.date+'T00:00:00').toLocaleDateString('en-IN',{weekday:'short',day:'numeric',month:'short'})} • {x.subtitle}</small></div></div>)}</section>)}</div>}
