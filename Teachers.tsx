import {subjects} from '../data/subjects';import {schedule} from '../data/schedule';
export function Teachers(){const teachers=[...new Map(subjects.map(s=>[s.teacher,s])).values()];return <div className="page"><div className="page-title"><span className="eyebrow">FACULTY</span><h1>Teachers</h1><p>Faculty linked to the official first-semester timetable.</p></div><div className="teacher-list">{teachers.map(s=>{const count=schedule.filter(e=>e.code===s.code).length;return <article className="teacher-card" key={s.teacher}><div className="avatar">{s.abbr.slice(0,2)}</div><div><b>{s.teacher}</b><span>{s.abbr} • {count} timetable entries</span><p>{s.name}</p></div></article>})}</div></div>}
